import React, { Fragment } from 'react';
import useStyles from './Post.styles';

const Post = () => {
	const classes = useStyles();

	return (
		<Fragment>
			<h1>POST</h1>
		</Fragment>
	);
};

export default Post;
